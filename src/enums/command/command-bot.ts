export enum CommandBot {
  Start = 'start',
  Support = 'support',
  FaqBase = 'faq',
  FaqStatuses = 'faq_statuses',
  Schedule = 'schedule',
  Contacts = 'contacts',
  OrdersList = 'orders_list',
}
